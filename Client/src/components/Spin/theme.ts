import { ThemeConfig } from "antd";
import { SpinColor } from "types/theme";

export const lightTheme: ThemeConfig = {
    token: {
        colorPrimary: SpinColor.light_primary
    }
}

export const darkTheme: ThemeConfig = {
    token: {
        colorPrimary: SpinColor.dark_primary
    }
}